1.此目录用以存放WEB系统的静态文件部分，此部分可以用在CDN中，所以得名。

2.此目录下的文件要求以WEB-INF/v目录中的jsp对应存放，每一个jsp页面对应一个目录，如singnin.jsp是
player模块下的，所以对应的静态文件目录如下：
/cdn/player/sigin
			 |
			 |-- css
			 |-- img
			 |-- js
当jsp页面中引用的静态资源很少时，也可将所有的静态资源文件一并放入到singn目录即可。

3.此目录有p目录用以存放整个平台都可以引用的静态资源，包括js css及img等静态文件，考虑到诸如jquery.js等
非常常用的类库级的js引用方便，我们将其直接存放入此目录下。
